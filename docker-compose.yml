version: '3.8'

services:
  storage-bucket:
    build: .
    ports:
      - "5100:5100"
    volumes:
      - ./uploads:/app/uploads
    environment:
      - NODE_ENV=production
      - PORT=5100
    restart: unless-stopped
